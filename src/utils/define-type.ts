export enum clientType {
    WEB = "web",
    APP = "app"
}

export enum userType {
    KAKAO = "kakao",
    GOOGLE = "google",
    ANYWHERE = "anywhere",
    All = "all"
};

export enum verifyResult {
    VERIFIED = "verified",
    EXPIRED = "expired",
    INVALID = "invalid"
};

